#include "oclib.h"

struct node{
    int foo;
    node link;
}

node func(node head, int length){
    int a = 0;
    string b = "";
    node c = new node;
    if (a < 3) {
        a = length;
        c = c.link;
    }
    else{
        if (0 == 0)
            a = 8;
        else
            b = "foo";
    }
}
